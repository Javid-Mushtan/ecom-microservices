spring:
  application:
    name: order-service

management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

logging:
  level:
    root: INFO
    com.ecommerce.order: DEBUG
  file:
    name: C:/Users/MMM JAVID/Desktop/ecom-microservices/logs/order-service.log